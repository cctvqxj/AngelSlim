global:
  save_path: ./output_mcore_qad/qwen3_moe_w4a16

model:
  # MCoreQAD dispatches by model_type in config.json; name is descriptive only.
  name: Qwen
  model_path: /path/to/Qwen3-30B-A3B

compression:
  name: MCoreQAD
  MCoreQAD:
    checkpoint_path: /path/to/mcore-checkpoint/qwen3-moe
    checkpoint_conversion_cpu: false
    # INT4 per-group-128 weights with BF16 activations (weight-only).
    format: w4a16
    init_scales_path: null
    lm_weight: 1.0
    distill_weight: 0.0
    distill_type: kl
    distill_temperature: 1.0
    distill_topk: 0
    experts_only: false
    seq_len: 2048
    micro_batch_size: 8
    train_iters: 200
    recompute: true
    save_every: 0
    parallel:
      tensor_parallel: 1
      pipeline_parallel: 1
      expert_parallel: 8
      context_parallel: 1
      sequence_parallel: false
    optim:
      lr: 2.0e-4
      weight_decay: 0.0
      betas: [0.9, 0.95]
      grad_clip: 1.0

dataset:
  name: TextDataset
  data_path: /path/to/sharegpt.jsonl
